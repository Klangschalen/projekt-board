name: Security - gitleaks

on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]
  schedule:
    - cron: "17 4 * * *"

permissions:
  contents: read
  pull-requests: read

jobs:
  gitleaks:
    name: gitleaks-scan
    uses: Klangschalen/.github/.github/workflows/gitleaks-scan.yml@3797874aff12223bcf9f588a5a61a5ee8014943c
    secrets: inherit
    with:
      fail-on-detection: false
