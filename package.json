{
  "name": "sound-spirit-projekt-board",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "node scripts/sync-local-data.js",
    "dev": "node scripts/sync-local-data.js && vite",
    "build": "node scripts/sync-local-data.js && vite build",
    "preview": "vite preview",
    "test": "echo \"Tests pending\" && exit 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "vite": "^8.2.2"
  }
}
